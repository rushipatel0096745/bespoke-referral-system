{
  "name": "referral-system",
  "version": "1.0.0",
  "description": "Dynamic `/memory-catcher/:slug` referral pages with Stripe attribution. One Edge Function serves every slug; the referral survives to Stripe metadata and lands in the hub commission dashboard. No per-page files, no per-referrer deploys.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "stripe": "^22.3.2"
  }
}
